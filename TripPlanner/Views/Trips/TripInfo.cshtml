@using TripPlanner.Models

@model BasicInfo

<h1 style="text-align:center">@Model.CityState.city, @Model.CityState.state</h1>
<br />
<br />
<h3 style="font-family:Arial">Latest Covid Informtion for @Model.CityState.city:</h3>
@foreach (var c in Model.Covid.counties)
{
    foreach (var d in c.historicData)
    {
        <p>
            Date: @d.date<br />
            Positive Count Total (to date): @d.positiveCt<br />
            Death Count Total (to date): @d.deathCt
        </p>

    }
}
<br />
<br />
<h3>Restaurants in @Model.CityState.city</h3>
<p>
    During your trip you are sure to get hungry!  Check out all of the great local cuisine here:
    <button onclick="location.href='/Trips/Restaurants?lat=@Model.CityState.lat&lng=@Model.CityState.lng&city=@Model.CityState.city'">Restaurants</button>
</p>
<br />
<h3>Accomodations in @Model.CityState.city</h3>
<p>Need a place to stay?  There is somewhere to get a relaxing night rest for any budget:
    <button onclick="location.href='/Trips/Lodging?lat=@Model.CityState.lat&lng=@Model.CityState.lng&city=@Model.CityState.city'">Accomodations</button></p>
<br />
<h3>Attractions in @Model.CityState.city</h3>
<p>No trip is complete without seeing all of the tourist attractions!  No matter what your hobbies are, you will be sure to find something to do!:
    <button onclick="location.href='/Trips/Attractions?lat=@Model.CityState.lat&lng=@Model.CityState.lng&city=@Model.CityState.city'">Attractions</button> </p>